# =====================================================================
# FKGIS runtime dependencies (web app + NLP pipeline)
# No database drivers are required — sessions are stored as JSON on disk.
# =====================================================================

# ---- Core NLP pipeline ----
spacy>=3.7
spacy-transformers          # transformer model (en_core_web_trf) for NER/deps

# ---- Graph construction & analytics ----
networkx                    # knowledge graph construction / PageRank
scipy                       # networkx.pagerank backend
matplotlib                  # paper-quality static graph rendering

# ---- LLM refinement (Gemini, same model family as current implementation) ----
google-genai                # optional: only used when GEMINI_API_KEY is set

# ---- Web application (session-based, no database) ----
fastapi
uvicorn[standard]
python-multipart            # multipart file uploads
python-dotenv               # load GEMINI_API_KEY / FKGIS_* from a local .env

# ---- Testing ----
pytest
httpx                       # required by fastapi's TestClient